doctype html 

html
    head 
        link(rel='stylesheet' href='/css/index.css')
        //- <meta name="viewport" content="width=device-width,initial-scale=1.0">
        meta(name="viewport" content="width=device-width,initial-scale=1.0")

    body
        div.navbar
            h2.navlogo='Inventory'

            a(href='/').url.navbtn
                 h5='Home'
            
            a(href='/About').url.navbtn
                 h5='About'
                 
            a(href='/login').url.navbtn
                h5='Login/Logout'
        
        div.container
            block content 

        div.footer
            p='Footer content'

    script(src='/scripts/index.js')